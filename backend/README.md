# Backend

This is the FastAPI backend for FragilityGraph.

## Running the Server

Per the project requirements, the server must be run using:

```bash
cd backend
python -m app.main
```

It will run on the port specified in `.env` (default 8000) and will auto-serve the frontend application.
